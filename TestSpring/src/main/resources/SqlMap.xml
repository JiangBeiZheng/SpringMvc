<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.dao.mapper.SqlMapper">
    <!-- 根据用户id查询 -->
    <select id="queryUserById" parameterType="int" resultType="Base">
		select * from base_dict where dict_id = #{id}
	</select>
    <select id="queryById" parameterType="int" resultType="Base">
        select * from base_dict where dict_id = #{id}
    </select>
</mapper>
